---
title: Calendar
current-date: 1305/1/11
---
# Ã°Å¸â€œâ€¦ The Calendar of Ghibalia

_Track the turning of seasons, sacred days, and the unfolding of history._

<div id="ghibalia-calendar-root"></div> <style> @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Lato:wght@300;400;700&display=swap'); #ghibalia-calendar-root { --gc-bg: #fdf6ec; --gc-surface: #fffdf8; --gc-border: #d6c9a8; --gc-accent-green: #7aab8a; --gc-accent-gold: #c9a84c; --gc-accent-rose: #d4826a; --gc-accent-sky: #7aaec9; --gc-text: #3d3222; --gc-text-muted: #8c7a5e; --gc-holiday: #b5603a; --gc-event: #4a7a63; --gc-radius: 14px; --gc-shadow: 0 4px 24px rgba(90,70,40,0.10); font-family: 'Lato', sans-serif; color: var(--gc-text); max-width: 780px; margin: 2rem auto; background: var(--gc-bg); border-radius: var(--gc-radius); box-shadow: var(--gc-shadow); border: 1.5px solid var(--gc-border); overflow: hidden; } .gc-header { background: linear-gradient(135deg, #a8c9a0 0%, #c9e0b8 40%, #e8d5a3 100%); padding: 1.6rem 2rem 1.2rem; display: flex; align-items: center; justify-content: space-between; border-bottom: 1.5px solid var(--gc-border); } .gc-header-title { font-family: 'Playfair Display', serif; font-size: 1.55rem; font-weight: 600; color: #2a3d28; letter-spacing: 0.01em; line-height: 1.2; } .gc-header-sub { font-size: 0.78rem; color: #4a6644; font-style: italic; margin-top: 2px; } .gc-nav { display: flex; align-items: center; gap: 0.6rem; } .gc-nav button { background: rgba(255,255,255,0.55); border: 1.5px solid #b5c9a8; border-radius: 50%; width: 34px; height: 34px; cursor: pointer; font-size: 1.1rem; color: #2a3d28; display: flex; align-items: center; justify-content: center; transition: background 0.18s, transform 0.12s; line-height: 1; } .gc-nav button:hover { background: rgba(255,255,255,0.85); transform: scale(1.08); } .gc-year-display { font-family: 'Playfair Display', serif; font-size: 0.95rem; color: #2a3d28; font-weight: 600; min-width: 60px; text-align: center; } .gc-month-tabs { display: flex; overflow-x: auto; scrollbar-width: none; padding: 0.7rem 1.2rem 0; gap: 0.35rem; background: var(--gc-surface); border-bottom: 1.5px solid var(--gc-border); } .gc-month-tabs::-webkit-scrollbar { display: none; } .gc-month-tab { flex-shrink: 0; padding: 0.3rem 0.85rem; border-radius: 20px; border: 1.5px solid transparent; background: transparent; font-size: 0.8rem; font-family: 'Lato', sans-serif; color: var(--gc-text-muted); cursor: pointer; transition: all 0.18s; white-space: nowrap; } .gc-month-tab:hover { border-color: var(--gc-border); color: var(--gc-text); background: #f0ebe0; } .gc-month-tab.active { background: var(--gc-accent-green); color: #fff; border-color: var(--gc-accent-green); font-weight: 700; } .gc-body { padding: 1.4rem 1.6rem 1rem; background: var(--gc-surface); } .gc-month-label { font-family: 'Playfair Display', serif; font-size: 1.25rem; font-weight: 600; color: var(--gc-text); margin-bottom: 0.3rem; } .gc-month-desc { font-size: 0.8rem; color: var(--gc-text-muted); font-style: italic; margin-bottom: 1rem; } .gc-weekdays { display: grid; grid-template-columns: repeat(5, 1fr); gap: 4px; margin-bottom: 4px; } .gc-weekday { text-align: center; font-size: 0.7rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--gc-text-muted); padding: 0.2rem 0; } .gc-days { display: grid; grid-template-columns: repeat(5, 1fr); gap: 4px; } .gc-day { min-height: 52px; border-radius: 9px; padding: 0.3rem 0.4rem; background: transparent; border: 1.5px solid transparent; cursor: default; position: relative; transition: background 0.15s, border-color 0.15s; } .gc-day.has-event { background: #eef6f0; border-color: #c0dcc8; cursor: pointer; } .gc-day.has-event:hover { background: #ddf0e4; border-color: var(--gc-accent-green); } .gc-day.is-holiday { background: #fdf0e8; border-color: #e8c9a8; cursor: pointer; } .gc-day.is-holiday:hover { background: #fce3cc; border-color: var(--gc-accent-rose); } .gc-day.empty { background: transparent; border-color: transparent; } .gc-day-num { font-size: 0.82rem; font-weight: 600; color: var(--gc-text-muted); line-height: 1; } .gc-day.has-event .gc-day-num { color: var(--gc-event); } .gc-day.is-holiday .gc-day-num { color: var(--gc-holiday); } .gc-day-dots { display: flex; gap: 3px; margin-top: 4px; flex-wrap: wrap; } .gc-dot { width: 6px; height: 6px; border-radius: 50%; flex-shrink: 0; } .gc-dot.event { background: var(--gc-accent-green); } .gc-dot.holiday { background: var(--gc-accent-rose); } .gc-day-label { font-size: 0.62rem; color: var(--gc-text-muted); margin-top: 2px; line-height: 1.2; overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; } .gc-day.has-event .gc-day-label { color: var(--gc-event); } .gc-day.is-holiday .gc-day-label { color: var(--gc-holiday); } /* Popup panel */ .gc-popup { display: none; position: fixed; z-index: 9999; background: var(--gc-surface); border: 1.5px solid var(--gc-border); border-radius: var(--gc-radius); box-shadow: 0 8px 36px rgba(80,60,30,0.18); padding: 1.2rem 1.4rem; max-width: 300px; min-width: 220px; pointer-events: none; } .gc-popup.visible { display: block; pointer-events: auto; animation: gc-fadein 0.16s ease; } @keyframes gc-fadein { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } } .gc-popup-date { font-family: 'Playfair Display', serif; font-size: 1rem; font-weight: 600; color: var(--gc-text); margin-bottom: 0.7rem; padding-bottom: 0.5rem; border-bottom: 1px solid var(--gc-border); } .gc-popup-item { margin-bottom: 0.7rem; } .gc-popup-item:last-child { margin-bottom: 0; } .gc-popup-type { font-size: 0.65rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 2px; } .gc-popup-type.event { color: var(--gc-event); } .gc-popup-type.holiday { color: var(--gc-holiday); } .gc-popup-name { font-size: 0.9rem; font-weight: 600; color: var(--gc-text); margin-bottom: 2px; } .gc-popup-desc { font-size: 0.78rem; color: var(--gc-text-muted); line-height: 1.45; } .gc-popup-close { position: absolute; top: 0.7rem; right: 0.8rem; background: none; border: none; font-size: 1.1rem; cursor: pointer; color: var(--gc-text-muted); line-height: 1; } .gc-legend { display: flex; gap: 1.2rem; padding: 0.8rem 1.6rem; border-top: 1.5px solid var(--gc-border); background: var(--gc-bg); } .gc-legend-item { display: flex; align-items: center; gap: 0.4rem; font-size: 0.74rem; color: var(--gc-text-muted); } .gc-legend-dot { width: 9px; height: 9px; border-radius: 50%; } </style> <script> (function() { // ============================================================ // CALENDAR DATA Ã¢â‚¬â€ edit these to customise your world calendar // ============================================================ // current in-world year label const YEAR_NUM = 1305 // numeric year const MONTHS = [ { name: "Month 1", days: 30, desc: "Add your month name and description here." }, { name: "Month 2", days: 30, desc: "Add your month name and description here." }, { name: "Month 3", days: 30, desc: "Add your month name and description here." }, { name: "Month 4", days: 30, desc: "Add your month name and description here." }, { name: "Month 5", days: 30, desc: "Add your month name and description here." }, { name: "Month 6", days: 30, desc: "Add your month name and description here." }, { name: "Month 7", days: 30, desc: "Add your month name and description here." }, { name: "Month 8", days: 30, desc: "Add your month name and description here." }, ]; // Replace Day 1Ã¢â‚¬â€œ5 names with your world's weekday names const WEEKDAYS = ["Day 1", "Day 2", "Day 3", "Day 4", "Day 5"]; // Events: { month (0-indexed, so 0Ã¢â‚¬â€œ7), day (1Ã¢â‚¬â€œ30), type: 'event'|'holiday', name, desc } // Add your own events here! Remove or replace these examples. const EVENTS = [ { month:0, day:1, type:'holiday', name:"New Year's Celebration", desc:"The first day of the year. Add your world's tradition here." }, { month:0, day:15, type:'event', name:"Example Lore Event", desc:"Something significant happened here. Replace with your own." }, { month:3, day:8, type:'holiday', name:"Midsummer Festival", desc:"A festival at the midpoint of the year. Customise as needed." }, { month:5, day:22, type:'event', name:"Example Event", desc:"Another placeholder. Fill in your campaign's history." }, { month:7, day:30, type:'holiday', name:"Year's End", desc:"The final day of the year. Replace with your world's tradition." }, ]; // ============================================================ // RENDER ENGINE // ============================================================ let currentMonth = 0;  const CURRENT_DAY = 10; function getEventsForDay(month, day) { return EVENTS.filter(e => e.month === month && e.day === day); } function buildCalendar() { const root = document.getElementById('ghibalia-calendar-root'); root.innerHTML = ''; // Header const header = el('div', 'gc-header'); const titleWrap = el('div'); titleWrap.innerHTML = `<div class="gc-header-title">Ã°Å¸â€œÅ“ Ghibalia World Calendar</div> <div class="gc-header-sub">${YEAR_NAME} &mdash; Year ${YEAR_NUM}</div>`; const nav = el('div', 'gc-nav'); const prevBtn = el('button'); prevBtn.innerHTML = 'Ã¢â‚¬Â¹'; const yearDisp = el('div', 'gc-year-display'); yearDisp.textContent = `${YEAR_NUM}`; const nextBtn = el('button'); nextBtn.innerHTML = 'Ã¢â‚¬Âº'; prevBtn.onclick = () => { currentMonth = (currentMonth - 1 + MONTHS.length) % MONTHS.length; buildCalendar(); }; nextBtn.onclick = () => { currentMonth = (currentMonth + 1) % MONTHS.length; buildCalendar(); }; nav.append(prevBtn, yearDisp, nextBtn); header.append(titleWrap, nav); root.append(header); // Month tabs const tabs = el('div', 'gc-month-tabs'); MONTHS.forEach((m, i) => { const tab = el('button', 'gc-month-tab'); tab.textContent = m.name; if (i === currentMonth) tab.classList.add('active'); tab.onclick = () => { currentMonth = i; buildCalendar(); }; tabs.append(tab); }); root.append(tabs); // Body const body = el('div', 'gc-body'); const month = MONTHS[currentMonth]; const mlabel = el('div', 'gc-month-label'); mlabel.textContent = month.name; const mdesc = el('div', 'gc-month-desc'); mdesc.textContent = month.desc; body.append(mlabel, mdesc); // Weekday headers const wdRow = el('div', 'gc-weekdays'); WEEKDAYS.forEach(d => { const wd = el('div', 'gc-weekday'); wd.textContent = d; wdRow.append(wd); }); body.append(wdRow); // Days grid (starting offset: use month index as a simple offset so months feel different) const startOffset = (currentMonth * 2) % 5; const grid = el('div', 'gc-days'); for (let i = 0; i < startOffset; i++) { grid.append(el('div', 'gc-day empty')); } for (let d = 1; d <= month.days; d++) { const events = getEventsForDay(currentMonth, d); const holidays = events.filter(e => e.type === 'holiday'); const lore = events.filter(e => e.type === 'event'); const dayEl = el('div', 'gc-day'); if (holidays.length) dayEl.classList.add('is-holiday'); else if (lore.length) dayEl.classList.add('has-event'); const numElf = el('div', 'gc-day-num'); numEl.textContent = d; if (d === CURRENT_DAY && currentMonth === 0) { dayEl.style.outline = '2px solid #c9a84c'; dayEl/style.outlineOffset = '-2px' ;} dayEl.append(numEl); if (events.length) { const dots = el('div', 'gc-day-dots'); events.forEach(ev => { const dot = el('div', 'gc-dot'); dot.classList.add(ev.type === 'holiday' ? 'holiday' : 'event'); dots.append(dot); }); dayEl.append(dots); const firstLabel = events[0].name; const labelEl = el('div', 'gc-day-label'); labelEl.textContent = firstLabel; dayEl.append(labelEl); dayEl.onclick = (e) => showPopup(e, currentMonth, d, events); } grid.append(dayEl); } body.append(grid); root.append(body); // Legend const legend = el('div', 'gc-legend'); legend.innerHTML = ` <div class="gc-legend-item"><div class="gc-legend-dot" style="background:#7aab8a"></div> Lore Event</div> <div class="gc-legend-item"><div class="gc-legend-dot" style="background:#d4826a"></div> Holiday / Festival</div> `; root.append(legend); // Popup container (outside root to avoid clipping) if (!document.getElementById('gc-popup')) { const popup = document.createElement('div'); popup.id = 'gc-popup'; popup.className = 'gc-popup'; popup.innerHTML = '<button class="gc-popup-close" onclick="document.getElementById(\'gc-popup\').classList.remove(\'visible\')">Ã¢Å“â€¢</button><div id="gc-popup-inner"></div>'; document.body.append(popup); document.addEventListener('click', (e) => { if (!e.target.closest('#gc-popup') && !e.target.closest('.gc-day.has-event, .gc-day.is-holiday')) { popup.classList.remove('visible'); } }); } // Scroll active tab into view const activeTab = tabs.querySelector('.active'); if (activeTab) activeTab.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' }); } function showPopup(e, month, day, events) { const popup = document.getElementById('gc-popup'); const inner = document.getElementById('gc-popup-inner'); const monthName = MONTHS[month].name; inner.innerHTML = `<div class="gc-popup-date">Day ${day} of ${monthName}, ${YEAR_NUM}</div>`; events.forEach(ev => { const item = document.createElement('div'); item.className = 'gc-popup-item'; item.innerHTML = ` <div class="gc-popup-type ${ev.type === 'holiday' ? 'holiday' : 'event'}">${ev.type === 'holiday' ? 'Ã¢Å“Â¦ Festival / Holiday' : 'Ã¢â€”â€  Lore Event'}</div> <div class="gc-popup-name">${ev.name}</div> <div class="gc-popup-desc">${ev.desc}</div> `; inner.append(item); }); // Position popup near click const rect = e.currentTarget.getBoundingClientRect(); const scrollY = window.scrollY || window.pageYOffset; let top = rect.bottom + scrollY + 8; let left = rect.left + window.scrollX; const popupW = 300; if (left + popupW > window.innerWidth - 16) left = window.innerWidth - popupW - 16; popup.style.top = top + 'px'; popup.style.left = left + 'px'; popup.classList.add('visible'); e.stopPropagation(); } function el(tag, cls) { const e = document.createElement(tag); if (cls) e.className = cls; return e; } // Boot if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', buildCalendar); } else { buildCalendar(); } })(); </script>